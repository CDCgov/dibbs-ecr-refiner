export function Configurations() {
  let title = `Your reportable condition configurations`
  let subtitle = `Set up reportable configurations herer to specifiy the data you'd like to retain in the refined eCRs for that condition.`

  return (
    <>
    <h1>{title}</h1>
    <p>{subtitle}</p>
    </>
  )
}
